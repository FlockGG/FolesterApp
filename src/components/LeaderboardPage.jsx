import { useCallback, useEffect, useState } from 'react'
import { ArrowUpRight, RefreshCw, Trophy } from 'lucide-react'
import { Link } from 'react-router-dom'
import Avatar from './Avatar'
import { useDatabase } from '../context/database'
import { getTipLeaderboard, getTipLeaderboardStats } from '../lib/data'

function formatNim(value) { return `${new Intl.NumberFormat('en-US', { maximumFractionDigits: 2 }).format(value || 0)} NIM` }
function shortAddress(address) { return address ? `${address.slice(0, 8)}…${address.slice(-5)}` : 'Wallet unavailable' }

function MetricSkeleton() { return <div className="animate-pulse rounded-2xl border border-slate-800 bg-black p-5"><span className="block h-3 w-24 rounded bg-white/10" /><span className="mt-4 block h-7 w-32 rounded bg-white/10" /><span className="mt-3 block h-3 w-3/4 rounded bg-white/10" /></div> }
function TableSkeleton() { return <div className="divide-y divide-slate-800" aria-busy="true" aria-label="Loading tipping rankings">{Array.from({ length: 8 }, (_, index) => <div key={index} className="flex items-center gap-4 px-4 py-4 sm:px-5 animate-pulse"><span className="h-4 w-6 rounded bg-white/10" /><span className="h-10 w-10 rounded-full bg-white/10" /><span className="h-3 flex-1 rounded bg-white/10" /><span className="h-4 w-20 rounded bg-white/10" /></div>)}</div> }

export default function LeaderboardPage() {
  const { client } = useDatabase()
  const [tab, setTab] = useState('earners')
  const [entries, setEntries] = useState([])
  const [stats, setStats] = useState(null)
  const [loading, setLoading] = useState(true)
  const [error, setError] = useState('')
  const load = useCallback(async () => {
    setLoading(true); setError('')
    try {
      const [nextEntries, nextStats] = await Promise.all([getTipLeaderboard(client, tab, 20), getTipLeaderboardStats(client)])
      setEntries(nextEntries); setStats(nextStats)
    } catch (nextError) { setEntries([]); setStats(null); setError(nextError?.message || 'The leaderboard could not be loaded.') } finally { setLoading(false) }
  }, [client, tab])
  useEffect(() => { load() }, [load])
  return <div className="page-enter mx-auto max-w-6xl px-5 py-10 sm:px-8 sm:py-14 md:px-10"><header className="flex flex-wrap items-end justify-between gap-5 border-b border-slate-800 pb-8"><div><p className="mono text-xs font-bold uppercase tracking-[0.18em] text-gold">Network analytics</p><h1 className="mt-3 text-4xl font-semibold tracking-tighter text-white sm:text-5xl">Network Tipping Leaderboard</h1><p className="mt-4 max-w-2xl text-sm leading-7 text-slate-400">On-chain Nimiq reward rankings for the researchers creating signal and the supporters funding it.</p></div><button type="button" onClick={load} disabled={loading} className="inline-flex min-h-10 items-center gap-2 rounded-xl border border-slate-800 bg-black px-3.5 text-xs font-bold text-slate-300 transition hover:border-slate-700 hover:text-white disabled:opacity-45"><RefreshCw size={15} className={loading ? 'animate-spin' : ''} />Refresh data</button></header><section className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3" aria-label="Tipping network summary">{loading && !stats ? <><MetricSkeleton /><MetricSkeleton /><MetricSkeleton /></> : <><MetricCard label="Total NIM distributed" value={formatNim(stats?.totalNim)} detail="Recorded Nimiq tip volume" /><MetricCard label="Total tip transactions" value={new Intl.NumberFormat('en-US').format(stats?.transactionCount || 0)} detail="Completed ledger records" /><MetricCard label="Top researcher" value={stats?.topResearcher?.username || 'No ranking yet'} detail={stats?.topResearcher ? formatNim(stats.topResearcher.total) : 'Awaiting first received tip'} /></>}</section><section className="mt-8 overflow-hidden rounded-2xl border border-slate-800 bg-black shadow-[0_14px_32px_rgba(0,0,0,0.24)]" aria-labelledby="rankings-title"><div className="flex flex-col gap-4 border-b border-slate-800 p-4 sm:flex-row sm:items-center sm:justify-between sm:px-5"><div><p className="mono text-[10px] font-bold uppercase tracking-[0.16em] text-gold">Rankings</p><h2 id="rankings-title" className="mt-1 text-lg font-bold text-white">Tip volume by participant</h2></div><div role="tablist" aria-label="Leaderboard category" className="grid grid-cols-2 rounded-lg border border-slate-800 bg-slate-950 p-1"><button role="tab" aria-selected={tab === 'earners'} onClick={() => setTab('earners')} className={`min-h-9 min-w-0 rounded-md px-3 text-xs font-bold transition ${tab === 'earners' ? 'bg-gold text-slate-950' : 'text-slate-400 hover:text-white'}`}>Top Earners</button><button role="tab" aria-selected={tab === 'supporters'} onClick={() => setTab('supporters')} className={`min-h-9 min-w-0 rounded-md px-3 text-xs font-bold transition ${tab === 'supporters' ? 'bg-gold text-slate-950' : 'text-slate-400 hover:text-white'}`}>Top Supporters</button></div></div><div className="hidden grid-cols-[4rem_minmax(0,1fr)_9rem_7rem] gap-4 border-b border-slate-800 px-5 py-3 text-[10px] font-bold uppercase tracking-[0.16em] text-slate-500 sm:grid"><span>Rank</span><span>Profile</span><span className="text-right">Tip volume</span><span /></div>{loading ? <TableSkeleton /> : error ? <div className="p-8 text-center"><p className="text-sm font-bold text-red-400">Unable to load network data.</p><p className="mt-1 text-xs text-slate-500">{error}</p><button type="button" onClick={load} className="mt-4 text-xs font-bold text-gold hover:underline">Try again</button></div> : entries.length === 0 ? <div className="flex min-h-56 flex-col items-center justify-center px-6 text-center"><Trophy size={24} className="text-slate-600" /><p className="mt-4 text-sm font-bold text-white">No network transactions recorded</p><p className="mt-2 max-w-sm text-xs leading-5 text-slate-400">Completed NIM tips will populate the ranking once they are recorded in the network ledger.</p></div> : <ol className="max-h-[46rem] divide-y divide-slate-800 overflow-y-auto">{entries.map((entry, index) => <li key={entry.id} className="grid grid-cols-[2.5rem_minmax(0,1fr)_auto] items-center gap-3 px-4 py-4 sm:grid-cols-[4rem_minmax(0,1fr)_9rem_7rem] sm:gap-4 sm:px-5"><span className="mono text-xs font-bold text-slate-500">#{index + 1}</span><Link to={`/profile/${entry.id}`} className="flex min-w-0 items-center gap-3 transition hover:opacity-80"><Avatar avatarUrl={entry.avatar_url} alt={entry.username || 'Folester member'} className="h-10 w-10 border border-white/10" /><span className="min-w-0"><span className="block truncate text-sm font-bold text-white">{entry.username || 'Folester member'}</span><span className="mono mt-0.5 block truncate text-[10px] text-slate-500">{shortAddress(entry.nimiq_address)}</span></span></Link><span className="mono text-right text-xs font-bold text-gold sm:text-sm">{formatNim(entry.total)}</span><Link to={`/profile/${entry.id}`} className="hidden min-h-9 items-center justify-center gap-1.5 rounded-lg border border-slate-800 px-2 text-[11px] font-bold text-slate-300 transition hover:border-gold/60 hover:text-gold sm:inline-flex">View <ArrowUpRight size={13} /></Link></li>)}</ol>}</section></div>
}

function MetricCard({ label, value, detail }) { return <article className="rounded-2xl border border-slate-800 bg-black p-5 shadow-[0_14px_32px_rgba(0,0,0,0.18)]"><p className="mono text-[10px] font-bold uppercase tracking-[0.16em] text-slate-500">{label}</p><p className="mono mt-3 truncate text-2xl font-bold tracking-tight text-white">{value}</p><p className="mt-2 text-xs text-slate-400">{detail}</p></article> }
